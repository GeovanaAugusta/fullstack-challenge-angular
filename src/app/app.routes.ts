import { Routes } from '@angular/router';
import { WeightCalculatorComponent } from './weight-calculator/weight-calculator.component';

export const routes: Routes = [
    { path: '', component: WeightCalculatorComponent } 
];
